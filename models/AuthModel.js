import db from '../database/connection.js';

export const schema = 'auth';
export const table = 'a_users';
export const schemaTable = schema+'.'+table;

class AuthModel{
   
}

export default new AuthModel();
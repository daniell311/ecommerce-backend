import db from '../database/connection.js';

export const tableAttr = {
    schema : 'auth',
    table : 'a_users',
    schemaTable : 'auth.a_users',
    key : 'userid'
}
class AuthModel{
   
}

export default new AuthModel();